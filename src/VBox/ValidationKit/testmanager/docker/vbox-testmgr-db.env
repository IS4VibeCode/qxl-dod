# Stuff which is needed for the Postgres container.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
# We need to have at least one database, but the Test Manager init script
# creates the actual database (called "testmanager") for us. So just use a
# dummy DB here.
POSTGRES_DB=dummy
POSTGRES_PORT=5432
