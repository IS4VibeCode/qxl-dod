# Using a separate volume for the Test Manager sources by default.
# Note: This will pull and work on the OSE version by default, so make
#       sure this exactly is what you want!
VBOX_TESTMGR_DATA=vbox-testmgr-web
